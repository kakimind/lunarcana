# 다음 프로젝트 시작할 때 부사수에게 전달할 것

PomPom Ville 작업하면서 겪은 실수·교훈 중, 이 프로젝트 한정이 아니라
**다른 앱을 새로 만들 때도 그대로 적용되는 것들**만 추려서 정리.

## 1. 성능/버그 디버깅

- **성능(렉/끊김) 판단은 반드시 profile 또는 release 빌드로만 할 것.**
  debug 빌드는 Dart JIT 인터프리터라 원래 훨씬 느림. 디버그 전용 로그
  확인하려고 잠깐 debug로 바꿨다가 "렉이 심해졌다"는 오해를 산 적 있음.
  로그 확인 끝나면 그 즉시 지우고 profile/release로 재빌드하는 걸
  습관화.

- **렌더링 프레임워크(Flame 등)에서 z-order/priority는 "낮을수록 아래,
  높을수록 위"** — 우선순위를 잘못 주면 컴포넌트가 완전히 가려져도
  에러 없이 그냥 안 보임. "코드는 맞는데 화면에 안 보인다"는 버그는
  로그로 안 잡히고 스크린샷/실기기로만 확인 가능하니, 이럴 땐 z-order부터
  의심할 것.

- **`render()`나 매 프레임 호출되는 곳에서 blur/shader/Path 같은 비싼
  연산을 매번 새로 만들지 말 것.** 입력이 안 바뀌면 최초 1회만 계산해서
  캐싱하고, 렌더는 캐시된 결과만 그리도록.

- **"비싼 연산 제거"와 "체감 부드러움"은 서로 다른 축.** blur를 없앴는데
  오히려 사용자가 "이전이 더 부드러웠다"고 한 적 있음(부드러운 가장자리가
  미세한 떨림을 덜 도드라져 보이게 하는 효과로 추정). 성능 최적화와
  체감 개선을 한 번에 묶어서 바꾸지 말고 하나씩 따로 검증.

- **`update(dt)`류 로직엔 dt 상한(clamp)을 기본으로 걸어둘 것.** 짧은
  스톨 뒤에 오는 큰 dt가 연속 애니메이션을 "순간이동"처럼 보이게 함.

- **"후보 중 처음 찾은 것만 쓰는" 패턴은 버그 소지가 큼.** 스캔 순서에
  의존하는 로직은 같은 상황에서도 다른 결과를 낼 수 있음 — 후보 전부를
  평가해서 최선을 고르는 방식으로.

- **런타임 버그는 코드만 읽고 추측하지 말고, 실제 로그로 정확한 상태를
  캡처한 뒤 그 값 그대로 넣은 독립 스크립트(`dart run` 등)로 기계적으로
  재현/검증할 것.** 코드 리딩만으로 "이러면 될 것 같다"보다 훨씬 빠르고
  확실함.

## 2. 실패하고 다른 방법을 찾은 경험

시행착오 자체를 기록해두면, 다음에 비슷한 증상을 만났을 때 "이거 예전에
겪었던 패턴이다"라고 바로 알아챌 수 있음. 실패한 시도도 그 자체로 정보.

- **버그인 줄 알았는데 사실 테스트 방법이 잘못됐던 경우.** 로비 스크롤을
  `adb shell input swipe`로 검증하다가 두 번 연속 "스크롤이 안 된다"고
  의심했는데, 알고 보니 뷰가 이미 스크롤 끝(현재 스테이지 근처)에 가 있는
  상태에서 같은 방향으로만 계속 스와이프했던 것 — 반대 방향으로 스와이프하니
  바로 정상 동작 확인됨. **"버그다"라고 결론 내리기 전에 반대 조건/반대
  방향도 한 번은 시도해볼 것.** 웹 빌드에서 "마우스 드래그가 안 된다"고
  느꼈던 것도 같은 종류의 방향 착각이었을 가능성이 있었음(마우스 휠은
  방향을 정확히 줬을 때 바로 됐음).

- **샌드박스 환경에서 `flutter run -d chrome`이 권한 문제로 실패.**
  안 되는 걸 억지로 고치려 하지 않고 `flutter build web` + 로컬 정적 서버로
  우회하는 대안 경로로 바로 전환. 대신 "이 환경 특유의 제약일 수 있다"는
  점을 기록해서, 실제 사용자 PC에서는 정상 동작할 가능성을 열어둠 —
  **환경 문제인지 코드 문제인지 구분해서 기록**하지 않으면 나중에 똑같은
  삽질을 반복하게 됨.

- **브라우저가 빌드 산출물(`main.dart.js`)을 캐싱해서 코드 수정이 반영 안
  된 것처럼 보였던 문제.** 서비스워커 unregister, `caches.delete()`,
  `localStorage.clear()`까지 다 해봤는데도 이전 응답이 계속 재사용되는
  경우가 있었음 — 결국 **매번 다른 포트로 정적 서버를 새로 띄우는 것**이
  유일하게 확실한 우회법이었음. 캐시 무효화를 아무리 시도해도 안 풀리면,
  "지우려" 하지 말고 아예 **새로운 URL/리소스 식별자**로 바꿔버리는 게
  더 빠를 때가 있음.

- **제스처 인식을 잘못 예측하고 코드부터 고쳤다가 다시 설계한 경우.**
  "제자리에서 눌렀다 떼도 발사되게 해달라"는 요청에 처음엔 최소 드래그
  거리 가드를 그냥 지웠는데도 안 됐음 — 원인 조사해보니 Flutter의
  `DragGestureRecognizer`는 포인터가 0px도 움직이지 않으면 `onDragStart`
  자체를 호출하지 않고 제스처를 스스로 거부해버리는 구조였음(가드 유무와
  무관). 결국 `TapCallbacks`를 별도로 믹스인해서 "움직이면 드래그로,
  제자리면 홀드 시간(스톱워치)으로 판정"하는 쪽으로 설계를 바꿔야 했음.
  **프레임워크의 제스처 인식기가 내부적으로 뭘 하는지 먼저 확인 안 하고
  "가드만 지우면 되겠지"라고 넘겨짚으면 헛수고할 수 있음.**

- **"예쁘게 만들어보려던" 디자인 변경을 되돌린 경우** (자세한 배경은
  아래 "디자인" 섹션 참고) — 인게임 배경을 로비처럼 밝게 바꿨다가, 사용자가
  "칙칙하다"고 해서 베이스 색을 흰색으로 교체했다가, 그래도 최종적으로는
  "이렇게 하면 예쁠 줄 알았는데 착각한 것 같다"는 사용자 판단으로 원래
  다크 톤으로 전부 되돌림. **여러 단계를 거쳐 되돌아왔다는 것 자체가
  실패가 아니라, "이 에셋은 애초에 다른 배경과 안 어울리는 방식으로
  디자인됐다"는 더 중요한 사실을 알아낸 과정**이었음 — 되돌린 이력을
  남겨두면 나중에 또 같은 시도를 반복하지 않게 됨.

- **재도전(이어하기) 기능을 넣었다가, 특정 실패 조건에서 무의미하다는
  걸 알게 된 경우.** "위험선을 넘어서 실패"한 상황에도 광고 보고 이어하기
  옵션을 띄웠는데, 이어해도 위험선 검사가 또 걸려서 즉시 재실패하는 버그가
  있었음 — 사용자가 실제로 겪고 나서야 "위험선 실패는 이어하기 자체가
  의미 없다"는 게 명확해짐. 결론적으로 **이어하기/재시도 옵션을 넣을 땐
  "이어해도 즉시 같은 이유로 다시 실패하지 않는지"를 조건별로 따로
  검증**해야 함 — 실패 사유가 여러 개면 그중 일부에만 재시도가 의미
  있을 수 있음.

- **자동화로 재현을 포기하고 사용자에게 직접 확인을 요청한 경우.** 드래그
  "진행 중"의 순간적 시각 상태(조준선 등)를 adb 합성 입력 + 타이밍 스크린샷
  조합으로 여러 번 재현 시도했지만 매번 타이밍이 안 맞거나 제스처 자체가
  다르게 인식됐음. 또 브라우저 자동화(CDP)로 만든 합성 포인터 이벤트는
  `isTrusted: false`라 Flutter 웹 엔진이 아예 무시하는 경우도 있었음(진짜
  하드웨어 이벤트만 처리) — 이런 땐 **자동화를 더 정교하게 만들려고
  시간을 쓰기보다, 사용자에게 직접 재현해달라고 요청하는 게 훨씬 빠르고
  확실함.** 언제 자동화를 포기해야 하는지 아는 것도 실력.

## 3. 다국어(i18n) 지원

- **`Row`의 직계 자식 `Text`는 `Expanded`/`Flexible` 없이 두면 위험.**
  비-flex `Row` 자식은 폭이 무제한이라 절대 줄바꿈되지 않고, 특정
  언어(독일어/폴란드어/일본어 전각 등)에서 문자열이 길어지는 순간
  RenderFlex overflow가 남. 새 UI 텍스트 추가할 때마다 체크할 포인트.

- **`EdgeInsets.only(left:/right:)`, `Alignment.centerLeft/Right`는
  RTL(아랍어 등)에서 물리적 좌우가 고정돼서 안 뒤집힘.** `Row`/
  `Directionality`는 자동으로 미러링되는데 이런 물리적 padding/alignment는
  안 따라감 → `EdgeInsetsDirectional`, `AlignmentDirectional` 사용.

## 4. 광고/수익화

- **"오프라인"과 "온라인인데 광고 미준비"는 다르게 취급해야 함.**
  둘 다 광고 없이 보상을 주면 오프라인이 가장 쉬운 우회로가 됨 —
  오프라인은 절대 보상 안 주고, 온라인인데 로드 실패는 1회만 봐주고
  그 다음부턴 막는 식으로 설계.

- **광고 SDK 초기화/로드 실패는 앱 시작을 막으면 안 됨** — try/catch로
  감싸고 실패 시 폴백 흐름으로 넘어가게.

- **"release 빌드 = 프로덕션 오픈"이 아님.** Play Console 내부 테스트용
  AAB도 `--release`라서, 별도 플래그(dart-define 등) 없이는 실기기
  테스터에게 실제 광고가 나갈 수 있음. 기본값은 항상 테스트 광고로 두고,
  실제 광고는 명시적 opt-in 빌드로만.

## 5. 스토어 등록/운영

- **Play Console에 AAB 올릴 때마다 versionCode(빌드 넘버)는 반드시
  증가.** 같은 번호로는 재업로드 자체가 거부됨 — 빌드 스크립트/체크리스트
  맨 앞에 넣어둘 것.

- **새 개발자 계정은 프로덕션 출시 전에 비공개 테스트(테스터 12명+,
  14일)를 강제로 거쳐야 함.** 일정 잡을 때 이 기간을 미리 계산에 넣을 것.

- **다른 플랫폼(예: 앱인토스 같은 미니앱 마켓)에 동시 등록을 고려할 때
  가장 먼저 확인할 것: 기술 스택 호환성(네이티브 앱이 그대로 올라가는지,
  웹/다른 프레임워크로 다시 만들어야 하는지), 그리고 사업자 등록 필요
  여부.** 게임 카테고리는 거의 항상 등급분류증명서가 필요하고, 이게
  사업자 등록을 전제로 하는 경우가 많음 — 개발 착수 전에 이것부터
  확인하면 뒤늦게 막히는 일을 피할 수 있음.

## 6. 디자인

- **아트 디렉션은 "가장 최근에 손댄 프로토타입"이 아니라 확정 문서를
  기준으로 삼을 것.** 이 프로젝트는 이미 Phaser로 크림/파스텔 톤 프로토타입이
  꽤 진행돼 있었는데, 실제 확정 아트 디렉션 문서는 다크 톤("유리구슬과
  심야")이었음 — 두 개가 어긋난 걸 늦게 발견해서 플랫폼(Phaser→Flutter)까지
  포함해 다시 시작해야 했음. 착수 전에 "지금 만들어진 것"과 "확정된 결정
  문서"가 같은 걸 가리키는지 반드시 대조.

- **색 구분이 게임플레이에 직접 쓰이는 요소(매치 색상 등)는 무광 파스텔을
  피할 것.** 아트 디렉션 검토 단계에서 "무광 파스텔은 6색 구분이 어렵고
  터뜨리는 쾌감도 약하다"는 이유로 명시적으로 탈락시킨 후보였음 — 귀엽게
  보이려고 채도를 낮추면 가독성/손맛이 같이 떨어지는 트레이드오프가 있다는
  걸 기억할 것.

- **에셋에 이미 녹아있는 톤을 배경/UI로 거스르려 하지 말 것.** 버블
  스프라이트 자체가 "다크 배경 위에서 빛나는 유리구슬"로 렌더링돼 있는데,
  그 위에 밝은 배경을 얹으려다 "예쁠 줄 알았는데 착각이었다"고 사용자가
  직접 판단을 내리고 되돌린 적 있음. 배경을 바꾸고 싶으면 배경만 바꾸는 게
  아니라 그 배경에 맞게 스프라이트 자체를 다시 렌더링해야 할 수도 있다는
  걸 먼저 검토.

- **화면(로비 vs 인게임)마다 톤이 달라도 됨 — 오히려 흔한 패턴.** 로비(탐험/
  맵 화면)는 밝고 화사하게, 실제 플레이 화면은 몰입감 있게 어둡게 가는 건
  불일치가 아니라 의도된 대비로 봐도 됨. "모든 화면 톤을 통일해야 한다"는
  전제를 당연하게 깔지 말 것.

- **텍스트 대비는 그림자만으로 보장되지 않음.** 동적이거나 다양한 배경
  (챕터별 배경 이미지, 그라디언트) 위에 얹는 텍스트는 `Shadow`/`blur`
  몇 겹으로는 부족한 경우가 실제로 있었음 — 최종적으로는 텍스트 뒤에
  불투명한 카드/칩 배경을 두거나 텍스트 색 자체를 완전 불투명한 진한
  색으로 바꿔야 확실히 해결됐음. 배경이 변할 수 있는 자리의 텍스트는
  "그림자로 때우기"보다 "불투명한 표면 위에 놓기"를 기본값으로.

- **색맹 등 접근성 보조는 "색상 차이"에 의존하지 않는 별도 신호(도형
  심볼 등)로.** 그리고 옵션으로 숨겨두기보다 기본 on으로 가는 게 실제
  사용성엔 더 나았음(이 프로젝트는 결국 항상 켜짐으로 감).

- **다국어 지원 예정이면 처음부터 "번역되면 텍스트가 길어진다"를 감안해서
  레이아웃을 짤 것.** 나중에 번역 붙이고서야 발견하는 overflow 버그
  (위 "다국어" 섹션)들의 근본 원인이 대부분 "처음 디자인할 때 한국어/영어
  기준 폭만 보고 여유 없이 짠 것"이었음.

- **폰트는 CDN 대신 로컬 에셋으로 번들.** 오프라인 플레이가 요구사항이면
  당연하지만, 그게 아니어도 로딩 지연으로 생기는 FOUT(폰트 깜빡임) 자체를
  없애는 효과도 있음.

- **"디자인 다듬어줘" 같은 폭넓은 요청을 받으면, 이미 확정된 색 팔레트/
  폰트는 건드리지 않고 restraint 원칙(입체감·광택·그림자 톤 매칭 같은
  디테일 추가)으로 접근하는 게 안전함.** 색상 자체를 바꾸는 건 이미 내려진
  결정을 뒤집는 거라 리스크가 크고, 대부분 "다듬어달라"는 요청의 실제
  의도는 톤 자체를 바꾸는 게 아니라 마감도를 올려달라는 것.

## 7. 이미지/에셋 자동화 작업

- **한글(또는 비ASCII) 파일명/경로를 다루는 스크립트는 인코딩 문제를
  겪기 쉬움.** Python이라면 `PYTHONUTF8=1`을 명시적으로 켜고, Windows에서
  콘솔 코드페이지가 다른 셸(Git Bash 등)이 아니라 PowerShell처럼 인코딩이
  맞는 셸로 실행할 것.

- **이미지 합성/제거 작업은 단순 색상 채우기보다 inpaint(주변 텍스처
  추정)가 자연스러움.** 그리고 텍스트/잉크 마스크를 만들 때 단순
  밝기 임계값 하나로는 anti-aliased 가장자리가 반쯤만 지워짐 —
  이진화 후 dilate + blur로 안티에일리어싱 halo까지 확실히 처리.

## 8. Windows 환경 특이사항

- **Git Bash(MSYS)에서 `adb`에 기기 절대경로(`/sdcard/...`)를 넘기면
  Windows 경로로 잘못 변환됨.** `MSYS_NO_PATHCONV=1` 접두사로 방지.

---
*PomPom Ville 프로젝트 작업 기록 기반, 2026-09-22 작성.*
